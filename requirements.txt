# ── Core ──────────────────────────────────────────────────────────────────────
numpy>=1.26.0,<2.1.0
streamlit>=1.40.0

# ── Model downloader (Google Drive) ───────────────────────────────────────────
gdown>=5.1.0

# ── Text / Image / Audio detection (TensorFlow + BOTH Keras backends) ─────────
# Models were saved with mixed Keras versions, so we keep both available and let
# utils.load_keras_model() pick the right one. TF_USE_LEGACY_KERAS is NOT set.
tensorflow==2.20.0
keras>=3.7.0       # Keras 3 — loads the image CNN (uses `batch_shape`)
tf-keras>=2.17.0   # Keras 2 fallback — loads the text LSTMs / Tokenizer pickle
openai>=1.0.0

# ── Image detection ───────────────────────────────────────────────────────────
Pillow>=10.0.0

# ── Video detection (ResNeXt50 + LSTM) ───────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0
opencv-python-headless>=4.8.0

# ── Audio detection ───────────────────────────────────────────────────────────
librosa>=0.10.0
matplotlib>=3.7.0
imageio[ffmpeg]
